"""VPN 출구 IP 배정 도구.

목적
    자체 운영하는 영상 서비스의 사용자들을 N 명씩 묶어, 한 묶음이 항상 같은
    출구 IP 로 나가도록 배정표를 만든다. 사용자는 WireGuard 클라이언트만
    설치하면 되고, 서비스 쪽에서 보면 한 묶음은 하나의 IP 로 보인다.

설계 원칙
    1. 결정적이다 — 같은 입력이면 항상 같은 배정표가 나온다. "1회성 통일"이
       목적이므로 재실행할 때마다 사람이 다른 IP 로 옮겨다니면 안 된다.
    2. 증분이다 — 기존 배정표를 입력으로 주면 이미 배정된 사용자는 절대
       움직이지 않고, 빠진 자리부터 채운다.
    3. 키는 만들지 않는다 — WireGuard 개인키는 이 도구가 생성하지 않는다.
       파이썬으로 암호키를 만드는 것보다 `wg genkey` 를 쓰는 편이 안전하다.
       설정 파일에는 자리표시자만 넣고, 키 생성 스크립트를 함께 내보낸다.
"""

from __future__ import annotations

__all__ = ["allocator", "capacity", "cost", "csvio", "models", "pool", "wireguard"]
